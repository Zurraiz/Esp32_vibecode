🟢 LEVEL 1.2: Making Changes Visible (Using Delay)
1. Introduction
In the previous lesson, you controlled an LED by turning it ON and OFF using blocks.
However, you may have noticed something unusual:
Even though you gave instructions to turn the LED ON and then OFF, it was difficult to clearly see both actions.
This happens because the ESP32 executes instructions extremely fast.
In this lesson, you will learn how to slow down the execution so that changes become visible and easier to understand.
2. Exploration
Available Blocks
Set Pin Mode
Digital Write HIGH
Digital Write LOW
Delay (ms)
Exploration Goal
In this phase, your goal is to experiment with delay and observe how it affects what you see.
You should:
Add delay between instructions
Change delay values
Observe how LED behavior changes
Compare results with and without delay
Suggested Exploration Questions
What happens if you turn the LED ON and then OFF without delay?
What happens when you add a delay between ON and OFF?
What happens if the delay is very small?
What happens if the delay is large?
Can you make the LED stay ON long enough to clearly see it?
Purpose of Exploration
The purpose of this phase is to understand that:
The instructions were always working correctly, but they were happening too fast to observe.

## 3. Concept Building


## 3.1 Why couldn’t you clearly see the ON and OFF?

In the previous lesson, when you turned the LED ON and then OFF, both actions happened almost instantly.
The ESP32 executes instructions extremely fast—far faster than the human eye can detect changes.
Because of this:
The LED turns ON
Then immediately turns OFF
These two actions happen so quickly that they appear as if they happened at the same time.
To you, it may look like:
Nothing happened
or
The LED did not turn ON properly
However, this is not the case. The ESP32 correctly executed both instructions. The change simply occurred too quickly to be observed.
This highlights an important idea:
The system is working correctly, but the result is not always visible to us.

## 3.2 What is Delay?

Delay is used to pause the execution of the program for a specific amount of time.
When the ESP32 reaches a Delay block, it temporarily stops executing further instructions before moving to the next step.
This pause gives enough time for the current action to be observed.
Delay is measured in milliseconds:
1000 milliseconds = 1 second
500 milliseconds = half a second
2000 milliseconds = 2 seconds
This allows you to control how long the system should wait before continuing.

## 3.3 Why do we need Delay?

We use delay to make changes visible and understandable.
Without delay:
Instructions execute almost instantly
Changes happen too quickly to observe
The result may appear incorrect or confusing
With delay:
The system pauses between actions
Each step becomes clearly visible
You can understand what each instruction is doing
For example:
If you:
Turn LED ON
Add a delay
Turn LED OFF
Then:
The LED remains ON for a short period
You can clearly see the ON state before it turns OFF
This makes the behavior of the system easier to understand.

## 3.4 What does Delay actually change?

Delay does not change the logic or purpose of the program.
It only changes the speed at which the program executes.
This means:
The sequence of instructions remains exactly the same
Only the time between instructions changes
For example:
Without delay:
With delay:
The instructions are identical, but the experience is different.

## 3.5 Understanding time and observation

Computers are designed to operate at extremely high speeds.
They can perform thousands or even millions of operations in a very short amount of time.
Humans, on the other hand, require time to notice and understand changes.
This creates a gap between:
Machine speed
Human perception
Delay acts as a bridge between these two.
It slows down execution so that:
Each step becomes observable
Each instruction becomes meaningful
Without delay, it becomes difficult to understand what the system is doing.

## 3.6 What actually happens when Delay is used?

When the ESP32 runs your program with delay:
It executes the first instruction
It reaches the Delay block
It pauses for the specified amount of time
After the pause, it continues to the next instruction
This process continues until all instructions are completed
During the delay:
No new instructions are executed
The current state remains unchanged
This pause allows you to clearly observe the effect of each instruction before moving on.

## 3.7 Key Insight of This Lesson

The ESP32 executes instructions very quickly
Rapid changes may not be visible to the human eye
Delay introduces a pause between instructions
This pause makes system behavior easier to observe
Delay does not change the logic of the program
It only changes the timing of execution
4. Student Task
Task: Make the LED Behavior Visible
Your objective is to make the LED’s ON and OFF actions clearly visible.
Requirements
Use Set Pin Mode correctly
Use Digital Write HIGH and LOW
Add at least one Delay block
Ensure delay is placed between ON and OFF
Expected Behavior
LED turns ON
Stays ON for a short time
Turns OFF
The change is clearly visible
5. Arduino Mapping
Generated Code

```
void setup() {
pinMode(2, OUTPUT);
}
void loop() {
digitalWrite(2, HIGH);
delay(1000);
digitalWrite(2, LOW);
}
```

Code Explanation

```cpp
pinMode(2, OUTPUT);
Configures pin 2 to control the LED
digitalWrite(2, HIGH);
Turns the LED ON
delay(1000);
Waits for 1 second so the LED remains visible
digitalWrite(2, LOW);
Turns the LED OFF
```

Execution Flow
Setup runs once
LED turns ON
System waits
LED turns OFF
Execution cycle completes
End of Lesson 1.2