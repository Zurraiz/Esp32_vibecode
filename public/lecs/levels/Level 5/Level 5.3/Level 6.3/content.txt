
## 🟠 LEVEL 6.3: Distance Measurement (Ultrasonic Sensor)


## 1. Introduction

In the previous lesson, you learned how the system can detect motion using a PIR sensor.
That was a binary understanding of the world:
Motion detected
No motion detected
In this lesson, the system becomes more precise.
Instead of just detecting whether something is present, the ESP32 will now measure how far away it is.
This is done using an ultrasonic sensor, which allows the system to understand space and distance in real time.

## 2. Exploration


## Available Blocks

Ultrasonic Sensor Read
Serial Print
(Optional) Conditional Logic

## Exploration Goal

Your goal is to observe how distance values change when objects move closer or farther away from the sensor.

## Try This

Activate the ultrasonic sensor
Print distance values continuously
Place your hand or object at different distances

## Observe

What value do you get when nothing is near the sensor?
How does the value change when you move closer?
How does it change when you move farther away?
Does the change feel smooth or sudden?

## Try Again

Move slowly toward the sensor
Then move quickly away
Compare how the values respond

## Purpose of Exploration

The goal is to understand that:
Systems can measure physical space
Distance is a continuously changing value
The environment can be quantified in real units

## 3. Concept Building


## 3.1 What is an ultrasonic sensor measuring?

An ultrasonic sensor does not detect presence or motion.
Instead, it measures distance by sending sound waves and calculating how long they take to return.
In simple terms:
It sends a signal
The signal reflects off an object
It measures how long the return takes
That time is converted into distance
This allows the system to understand how far away an object is.

## 3.2 Understanding distance as a value

Unlike previous sensors:
PIR → only detects motion
Analog input → measures intensity
Ultrasonic sensor → measures space
The output is a number that represents distance.
For example:
Small value → object is close
Large value → object is far
This introduces a new type of understanding:
👉 spatial awareness

## 3.3 Why does distance change smoothly?

As you move an object:
Distance decreases gradually when approaching
Distance increases gradually when moving away
This creates a smooth, continuous stream of values.
This is similar to analog behavior, but applied to physical space.

## 3.4 What does the ESP32 actually receive?

The sensor converts physical distance into a numeric value.
The ESP32 receives:
A number representing how far the object is
This allows the system to:
React to proximity
Measure space accurately
Make decisions based on distance

## 3.5 Why is this important?

Distance measurement is used in real systems such as:
Parking sensors in cars
Obstacle avoidance robots
Smart door systems
Level detection in tanks
These systems need to know not just if something exists, but how far it is.

## 3.6 From detection to measurement

At this point in your learning:
PIR sensor → detects motion
Analog sensor → measures intensity
Ultrasonic sensor → measures distance
This shows a progression:
👉 detection → measurement → precision control

## 3.7 How systems use distance values

Distance values are often used to:
Trigger actions when objects are too close
Adjust behavior based on proximity
Create safety responses
For example:
If object is too close → stop motor
If object is far → continue movement
This turns raw distance into decision-making logic.

## 3.8 Real-time behavior of distance systems

Since the sensor runs continuously:
Distance values are updated constantly
Movement instantly changes readings
The system always has the latest information
This makes it suitable for dynamic environments.

## 3.9 Key Insight of This Lesson

Ultrasonic sensors measure distance using sound waves
Output is a numeric value representing space
Distance changes continuously as objects move
Systems can now understand physical space
This enables proximity-based decision making
ESP32 continuously updates distance in real time

## 4. Student Task


## Task: Build a Distance Monitoring System

Your objective is to:
Read distance using ultrasonic sensor
Display values in real time
Observe how distance changes with movement

## Requirements

Use Ultrasonic Sensor Read
Use Serial Print
Move object closer and farther
Observe continuous updates

## Expected Behavior

Distance values update in real time
Smaller values when object is near
Larger values when object is far
Smooth transitions between values

## 5. Arduino Mapping


## Generated Code

#define TRIG_PIN 5
#define ECHO_PIN 18
void setup() {
Serial.begin(9600);
pinMode(TRIG_PIN, OUTPUT);
pinMode(ECHO_PIN, INPUT);
}
void loop() {
digitalWrite(TRIG_PIN, LOW);
delayMicroseconds(2);
digitalWrite(TRIG_PIN, HIGH);
delayMicroseconds(10);
digitalWrite(TRIG_PIN, LOW);
long duration = pulseIn(ECHO_PIN, HIGH);
float distance = duration * 0.034 / 2;
Serial.println(distance);
}

## Code Explanation

TRIG_PIN
Sends ultrasonic pulse
ECHO_PIN
Receives reflected signal
pulseIn()
Measures time taken for signal to return
distance calculation
Converts time into physical distance

## Execution Flow

Sensor sends ultrasonic pulse
Pulse reflects off object
Return time is measured
Distance is calculated
Value is printed
Process repeats continuously

## End of Lesson 6.3
